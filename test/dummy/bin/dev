#!/usr/bin/env ruby
require "fileutils"

APP_ROOT = File.expand_path("..", __dir__)
HOST = ENV.fetch("HOST", "0.0.0.0")

FileUtils.chdir APP_ROOT do
	system("bin/rails db:prepare", exception: true)
	exec "./bin/rails", "server", "-b", HOST, *ARGV
end
